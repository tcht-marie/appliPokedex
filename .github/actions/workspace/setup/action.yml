name: 'Setup'
description: Setup
runs:
  using: "composite"
  steps:
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.24.0'
        channel: stable
        cache: true
    - name: Requirement
      run: dart tools/requirement_install.dart --ci
      shell: bash
